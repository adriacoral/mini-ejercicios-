using UnityEngine;

public class decimalesP : MonoBehaviour
{
    public float decimales = 0.05f;
    public float decimales2 = 0.09f;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        print(decimales + decimales2);
            
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
